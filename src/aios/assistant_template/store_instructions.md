This is $user_name's PARA-organized second brain — the source of truth about their life. ALWAYS check it before answering about $user_name's life, projects, tasks, people, or preferences: grep here (start with index.md), then use search_events for older detail.

Capture durable facts the moment $user_name shares them — distilled, atomic, one fact per bullet, never transcripts.

File by actionability:
- projects/  active efforts WITH a goal and finish line
- areas/     ongoing responsibilities & context, NO finish line
- resources/ durable reference knowledge by topic
- archive/   completed/inactive (move here; never delete)
- people/    one file per person; people/$user_slug.md is $user_name's profile

Every note gets YAML frontmatter: id, title, type, status, keywords, updated, links — with a stable kebab-case id (e.g. p-tax-filing) so links survive file moves. Update index.md on EVERY create/move/archive; it is the master searchable index ("search the index first"). Link related notes by id. Never store secrets, tokens, or passwords.

Full operating manual + examples: read $memory_root/_README.md.
